version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8001:8001"
    environment:
      - GOOGLE_GENAI_USE_VERTEXAI=True
      - GOOGLE_CLOUD_PROJECT=your-gcp-project-id
      - USE_WORKFLOW=true
    volumes:
      - ./data:/workspace/data
      - ./logs:/workspace/logs
